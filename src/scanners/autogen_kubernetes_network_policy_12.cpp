/// @file scanners/autogen_kubernetes_network_policy_12.cpp
/// @brief Auto-generated scanner: Kubernetes Network Policy (12)
///        Checks: Kubernetes network policy egre, Kubernetes network policy labe, Kubernetes network policy name, Kubernetes network policy pod , Kubernetes network policy port
#include "scanner_base.hpp"
#include <regex>
#include <chrono>

namespace apex {
namespace {

/// Kubernetes network policy egress bypass technique
std::vector<Finding> scan_kubernetes_network_policy_egress_bypass_(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Kubernetes network policy egress bypass technique
  auto resp = http.get(base + "/admin");
  if (resp.status_code == 403 || resp.status_code == 401) {
    // Try bypass
    auto bypass = http.get(base + "/admin", {{"X-Original-URL", "/admin"}});
    if (bypass.status_code == 200 && bypass.body.size() > 200 &&
        bypass.body.find("{") == 0) {
      findings.push_back(Finding{"Kubernetes network policy egress bypass technique", "critical", base + "/admin",
                          "Access control bypass successful",
                          "", "X-Original-URL", bypass.body.substr(0, 150)});
    }
  }

  return findings;
}

/// Kubernetes network policy label manipulation
std::vector<Finding> scan_kubernetes_network_policy_label_manipula(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Kubernetes network policy label manipulation
  // TODO: Implement specific check for: Kubernetes network policy label manipulation
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// Kubernetes network policy namespace bypass
std::vector<Finding> scan_kubernetes_network_policy_namespace_bypa(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Kubernetes network policy namespace bypass
  auto resp = http.get(base + "/admin");
  if (resp.status_code == 403 || resp.status_code == 401) {
    // Try bypass
    auto bypass = http.get(base + "/admin", {{"X-Original-URL", "/admin"}});
    if (bypass.status_code == 200 && bypass.body.size() > 200 &&
        bypass.body.find("{") == 0) {
      findings.push_back(Finding{"Kubernetes network policy namespace bypass", "critical", base + "/admin",
                          "Access control bypass successful",
                          "", "X-Original-URL", bypass.body.substr(0, 150)});
    }
  }

  return findings;
}

/// Kubernetes network policy pod selector confusion
std::vector<Finding> scan_kubernetes_network_policy_pod_selector_c(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Kubernetes network policy pod selector confusion
  // TODO: Implement specific check for: Kubernetes network policy pod selector confusion
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// Kubernetes network policy port range exploitation
std::vector<Finding> scan_kubernetes_network_policy_port_range_exp(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Kubernetes network policy port range exploitation
  // TODO: Implement specific check for: Kubernetes network policy port range exploitation
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// Kubernetes network policy CIDR manipulation
std::vector<Finding> scan_kubernetes_network_policy_cidr_manipulat(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Kubernetes network policy CIDR manipulation
  // TODO: Implement specific check for: Kubernetes network policy CIDR manipulation
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// Kubernetes network policy precedence exploitation
std::vector<Finding> scan_kubernetes_network_policy_precedence_exp(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Kubernetes network policy precedence exploitation
  // TODO: Implement specific check for: Kubernetes network policy precedence exploitation
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// Kubernetes network policy service mesh bypass
std::vector<Finding> scan_kubernetes_network_policy_service_mesh_b(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Kubernetes network policy service mesh bypass
  auto resp = http.get(base + "/admin");
  if (resp.status_code == 403 || resp.status_code == 401) {
    // Try bypass
    auto bypass = http.get(base + "/admin", {{"X-Original-URL", "/admin"}});
    if (bypass.status_code == 200 && bypass.body.size() > 200 &&
        bypass.body.find("{") == 0) {
      findings.push_back(Finding{"Kubernetes network policy service mesh bypass", "critical", base + "/admin",
                          "Access control bypass successful",
                          "", "X-Original-URL", bypass.body.substr(0, 150)});
    }
  }

  return findings;
}

/// Kubernetes network policy exception exploitation
std::vector<Finding> scan_kubernetes_network_policy_exception_expl(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Kubernetes network policy exception exploitation
  // TODO: Implement specific check for: Kubernetes network policy exception exploitation
  // This is a placeholder generated by auto-expand.py

  return findings;
}


} // namespace

std::vector<Scanner> register_autogen_kubernetes_network_policy_12_scanners() {
  return {
      {"Kubernetes network policy egress bypass technique", scan_kubernetes_network_policy_egress_bypass_},
      {"Kubernetes network policy label manipulation", scan_kubernetes_network_policy_label_manipula},
      {"Kubernetes network policy namespace bypass", scan_kubernetes_network_policy_namespace_bypa},
      {"Kubernetes network policy pod selector confusion", scan_kubernetes_network_policy_pod_selector_c},
      {"Kubernetes network policy port range exploitation", scan_kubernetes_network_policy_port_range_exp},
      {"Kubernetes network policy CIDR manipulation", scan_kubernetes_network_policy_cidr_manipulat},
      {"Kubernetes network policy precedence exploitation", scan_kubernetes_network_policy_precedence_exp},
      {"Kubernetes network policy service mesh bypass", scan_kubernetes_network_policy_service_mesh_b},
      {"Kubernetes network policy exception exploitation", scan_kubernetes_network_policy_exception_expl},
  };
}

} // namespace apex
