/// @file scanners/autogen_gcp_iam_metadata_12.cpp
/// @brief Auto-generated scanner: GCP IAM Metadata (12)
///        Checks: GCP metadata project-level SSH, GCP IAM organization policy by, GCP IAM conditional binding by, GCP IAM domain-wide delegation, GCP metadata service account s
#include "scanner_base.hpp"
#include <regex>
#include <chrono>

namespace apex {
namespace {

/// GCP metadata project-level SSH key injection
std::vector<Finding> scan_gcp_metadata_project_level_ssh_key_injec(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // GCP metadata project-level SSH key injection
  // TODO: Implement specific check for: GCP metadata project-level SSH key injection
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// GCP IAM organization policy bypass technique
std::vector<Finding> scan_gcp_iam_organization_policy_bypass_techn(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // GCP IAM organization policy bypass technique
  auto resp = http.get(base + "/admin");
  if (resp.status_code == 403 || resp.status_code == 401) {
    // Try bypass
    auto bypass = http.get(base + "/admin", {{"X-Original-URL", "/admin"}});
    if (bypass.status_code == 200 && bypass.body.size() > 200 &&
        bypass.body.find("{") == 0) {
      findings.push_back(Finding{"GCP IAM organization policy bypass technique", "critical", base + "/admin",
                          "Access control bypass successful",
                          "", "X-Original-URL", bypass.body.substr(0, 150)});
    }
  }

  return findings;
}

/// GCP IAM conditional binding bypass exploitation
std::vector<Finding> scan_gcp_iam_conditional_binding_bypass_explo(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // GCP IAM conditional binding bypass exploitation
  auto resp = http.get(base + "/admin");
  if (resp.status_code == 403 || resp.status_code == 401) {
    // Try bypass
    auto bypass = http.get(base + "/admin", {{"X-Original-URL", "/admin"}});
    if (bypass.status_code == 200 && bypass.body.size() > 200 &&
        bypass.body.find("{") == 0) {
      findings.push_back(Finding{"GCP IAM conditional binding bypass exploitation", "critical", base + "/admin",
                          "Access control bypass successful",
                          "", "X-Original-URL", bypass.body.substr(0, 150)});
    }
  }

  return findings;
}

/// GCP IAM domain-wide delegation abuse technique
std::vector<Finding> scan_gcp_iam_domain_wide_delegation_abuse_tec(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // GCP IAM domain-wide delegation abuse technique
  // TODO: Implement specific check for: GCP IAM domain-wide delegation abuse technique
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// GCP metadata service account scope enumeration
std::vector<Finding> scan_gcp_metadata_service_account_scope_enume(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // GCP metadata service account scope enumeration
  // TODO: Implement specific check for: GCP metadata service account scope enumeration
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// GCP IAM workload identity federation bypass
std::vector<Finding> scan_gcp_iam_workload_identity_federation_byp(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // GCP IAM workload identity federation bypass
  auto resp = http.get(base + "/admin");
  if (resp.status_code == 403 || resp.status_code == 401) {
    // Try bypass
    auto bypass = http.get(base + "/admin", {{"X-Original-URL", "/admin"}});
    if (bypass.status_code == 200 && bypass.body.size() > 200 &&
        bypass.body.find("{") == 0) {
      findings.push_back(Finding{"GCP IAM workload identity federation bypass", "critical", base + "/admin",
                          "Access control bypass successful",
                          "", "X-Original-URL", bypass.body.substr(0, 150)});
    }
  }

  return findings;
}


} // namespace

std::vector<Scanner> register_autogen_gcp_iam_metadata_12_scanners() {
  return {
      {"GCP metadata project-level SSH key injection", scan_gcp_metadata_project_level_ssh_key_injec},
      {"GCP IAM organization policy bypass technique", scan_gcp_iam_organization_policy_bypass_techn},
      {"GCP IAM conditional binding bypass exploitation", scan_gcp_iam_conditional_binding_bypass_explo},
      {"GCP IAM domain-wide delegation abuse technique", scan_gcp_iam_domain_wide_delegation_abuse_tec},
      {"GCP metadata service account scope enumeration", scan_gcp_metadata_service_account_scope_enume},
      {"GCP IAM workload identity federation bypass", scan_gcp_iam_workload_identity_federation_byp},
  };
}

} // namespace apex
