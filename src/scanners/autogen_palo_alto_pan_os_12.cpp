/// @file scanners/autogen_palo_alto_pan_os_12.cpp
/// @brief Auto-generated scanner: Palo Alto PAN-OS (12)
///        Checks: PAN-OS GlobalProtect exploitat, PAN-OS management interface ex, PAN-OS XML API exploitation te, PAN-OS Panorama exploitation m, PAN-OS WildFire evasion exploi
#include "scanner_base.hpp"
#include <regex>
#include <chrono>

namespace apex {
namespace {

/// PAN-OS GlobalProtect exploitation technique
std::vector<Finding> scan_pan_os_globalprotect_exploitation_techni(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // PAN-OS GlobalProtect exploitation technique
  // TODO: Implement specific check for: PAN-OS GlobalProtect exploitation technique
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// PAN-OS management interface exposure method
std::vector<Finding> scan_pan_os_management_interface_exposure_met(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // PAN-OS management interface exposure method
  // TODO: Implement specific check for: PAN-OS management interface exposure method
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// PAN-OS XML API exploitation technique
std::vector<Finding> scan_pan_os_xml_api_exploitation_technique(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // PAN-OS XML API exploitation technique
  // TODO: Implement specific check for: PAN-OS XML API exploitation technique
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// PAN-OS Panorama exploitation method
std::vector<Finding> scan_pan_os_panorama_exploitation_method(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // PAN-OS Panorama exploitation method
  // TODO: Implement specific check for: PAN-OS Panorama exploitation method
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// PAN-OS WildFire evasion exploitation method
std::vector<Finding> scan_pan_os_wildfire_evasion_exploitation_met(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // PAN-OS WildFire evasion exploitation method
  // TODO: Implement specific check for: PAN-OS WildFire evasion exploitation method
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// PAN-OS User-ID agent exploitation technique
std::vector<Finding> scan_pan_os_user_id_agent_exploitation_techni(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // PAN-OS User-ID agent exploitation technique
  // TODO: Implement specific check for: PAN-OS User-ID agent exploitation technique
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// PAN-OS zone-based policy bypass exploitation
std::vector<Finding> scan_pan_os_zone_based_policy_bypass_exploita(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // PAN-OS zone-based policy bypass exploitation
  auto resp = http.get(base + "/admin");
  if (resp.status_code == 403 || resp.status_code == 401) {
    // Try bypass
    auto bypass = http.get(base + "/admin", {{"X-Original-URL", "/admin"}});
    if (bypass.status_code == 200 && bypass.body.size() > 200 &&
        bypass.body.find("{") == 0) {
      findings.push_back(Finding{"PAN-OS zone-based policy bypass exploitation", "critical", base + "/admin",
                          "Access control bypass successful",
                          "", "X-Original-URL", bypass.body.substr(0, 150)});
    }
  }

  return findings;
}


} // namespace

std::vector<Scanner> register_autogen_palo_alto_pan_os_12_scanners() {
  return {
      {"PAN-OS GlobalProtect exploitation technique", scan_pan_os_globalprotect_exploitation_techni},
      {"PAN-OS management interface exposure method", scan_pan_os_management_interface_exposure_met},
      {"PAN-OS XML API exploitation technique", scan_pan_os_xml_api_exploitation_technique},
      {"PAN-OS Panorama exploitation method", scan_pan_os_panorama_exploitation_method},
      {"PAN-OS WildFire evasion exploitation method", scan_pan_os_wildfire_evasion_exploitation_met},
      {"PAN-OS User-ID agent exploitation technique", scan_pan_os_user_id_agent_exploitation_techni},
      {"PAN-OS zone-based policy bypass exploitation", scan_pan_os_zone_based_policy_bypass_exploita},
  };
}

} // namespace apex
