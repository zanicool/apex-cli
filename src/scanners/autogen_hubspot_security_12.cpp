/// @file scanners/autogen_hubspot_security_12.cpp
/// @brief Auto-generated scanner: HubSpot Security (12)
///        Checks: HubSpot workflow automation ma, HubSpot API scope escalation e, HubSpot webhook manipulation e, HubSpot custom object permissi
#include "scanner_base.hpp"
#include <regex>
#include <chrono>

namespace apex {
namespace {

/// HubSpot workflow automation manipulation
std::vector<Finding> scan_hubspot_workflow_automation_manipulation(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // HubSpot workflow automation manipulation
  // TODO: Implement specific check for: HubSpot workflow automation manipulation
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// HubSpot API scope escalation exploitation
std::vector<Finding> scan_hubspot_api_scope_escalation_exploitatio(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // HubSpot API scope escalation exploitation
  // TODO: Implement specific check for: HubSpot API scope escalation exploitation
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// HubSpot webhook manipulation exploitation
std::vector<Finding> scan_hubspot_webhook_manipulation_exploitatio(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // HubSpot webhook manipulation exploitation
  // TODO: Implement specific check for: HubSpot webhook manipulation exploitation
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// HubSpot custom object permission bypass
std::vector<Finding> scan_hubspot_custom_object_permission_bypass(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // HubSpot custom object permission bypass
  std::regex id_re(R"x(/api/[^/]+/(\d+))x");
  
  for (const auto &url : crawl.urls) {
    std::smatch m;
    if (!std::regex_search(url, m, id_re)) continue;
    int id = std::stoi(m[1].str());
    std::string base_path = url.substr(0, m.position(1));
    
    auto r1 = http.get(base_path + std::to_string(id + 1));
    if (r1.status_code == 200 && r1.body.find("{") == 0 &&
        r1.body.find("email") != std::string::npos) {
      findings.push_back(Finding{"HubSpot custom object permission bypass", "high", base_path + std::to_string(id + 1),
                          "IDOR: accessed another user\'s data by incrementing ID",
                          "id", std::to_string(id + 1), r1.body.substr(0, 150)});
      return findings;
    }
    break;
  }

  return findings;
}


} // namespace

std::vector<Scanner> register_autogen_hubspot_security_12_scanners() {
  return {
      {"HubSpot workflow automation manipulation", scan_hubspot_workflow_automation_manipulation},
      {"HubSpot API scope escalation exploitation", scan_hubspot_api_scope_escalation_exploitatio},
      {"HubSpot webhook manipulation exploitation", scan_hubspot_webhook_manipulation_exploitatio},
      {"HubSpot custom object permission bypass", scan_hubspot_custom_object_permission_bypass},
  };
}

} // namespace apex
