/// @file scanners/autogen_git_repository_security_12.cpp
/// @brief Auto-generated scanner: Git Repository Security (12)
///        Checks: Git exposed .git directory exp, Git object file download for s, Git pack file extraction explo, Git hook script injection expl, Git signed commit bypass explo
#include "scanner_base.hpp"
#include <regex>
#include <chrono>

namespace apex {
namespace {

/// Git exposed .git directory exploitation method
std::vector<Finding> scan_git_exposed_git_directory_exploitation_m(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Git exposed .git directory exploitation method
  // TODO: Implement specific check for: Git exposed .git directory exploitation method
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// Git object file download for source recovery
std::vector<Finding> scan_git_object_file_download_for_source_reco(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Git object file download for source recovery
  std::regex id_re(R"x(/api/[^/]+/(\d+))x");
  
  for (const auto &url : crawl.urls) {
    std::smatch m;
    if (!std::regex_search(url, m, id_re)) continue;
    int id = std::stoi(m[1].str());
    std::string base_path = url.substr(0, m.position(1));
    
    auto r1 = http.get(base_path + std::to_string(id + 1));
    if (r1.status_code == 200 && r1.body.find("{") == 0 &&
        r1.body.find("email") != std::string::npos) {
      findings.push_back(Finding{"Git object file download for source recovery", "high", base_path + std::to_string(id + 1),
                          "IDOR: accessed another user\'s data by incrementing ID",
                          "id", std::to_string(id + 1), r1.body.substr(0, 150)});
      return findings;
    }
    break;
  }

  return findings;
}

/// Git pack file extraction exploitation technique
std::vector<Finding> scan_git_pack_file_extraction_exploitation_te(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Git pack file extraction exploitation technique
  // TODO: Implement specific check for: Git pack file extraction exploitation technique
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// Git hook script injection exploitation method
std::vector<Finding> scan_git_hook_script_injection_exploitation_m(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Git hook script injection exploitation method
  // TODO: Implement specific check for: Git hook script injection exploitation method
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// Git signed commit bypass exploitation technique
std::vector<Finding> scan_git_signed_commit_bypass_exploitation_te(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Git signed commit bypass exploitation technique
  auto resp = http.get(base + "/admin");
  if (resp.status_code == 403 || resp.status_code == 401) {
    // Try bypass
    auto bypass = http.get(base + "/admin", {{"X-Original-URL", "/admin"}});
    if (bypass.status_code == 200 && bypass.body.size() > 200 &&
        bypass.body.find("{") == 0) {
      findings.push_back(Finding{"Git signed commit bypass exploitation technique", "critical", base + "/admin",
                          "Access control bypass successful",
                          "", "X-Original-URL", bypass.body.substr(0, 150)});
    }
  }

  return findings;
}

/// Git branch protection bypass exploitation
std::vector<Finding> scan_git_branch_protection_bypass_exploitatio(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Git branch protection bypass exploitation
  auto resp = http.get(base + "/admin");
  if (resp.status_code == 403 || resp.status_code == 401) {
    // Try bypass
    auto bypass = http.get(base + "/admin", {{"X-Original-URL", "/admin"}});
    if (bypass.status_code == 200 && bypass.body.size() > 200 &&
        bypass.body.find("{") == 0) {
      findings.push_back(Finding{"Git branch protection bypass exploitation", "critical", base + "/admin",
                          "Access control bypass successful",
                          "", "X-Original-URL", bypass.body.substr(0, 150)});
    }
  }

  return findings;
}

/// Git shallow clone limitation exploitation
std::vector<Finding> scan_git_shallow_clone_limitation_exploitatio(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Git shallow clone limitation exploitation
  // TODO: Implement specific check for: Git shallow clone limitation exploitation
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// Git worktree manipulation exploitation method
std::vector<Finding> scan_git_worktree_manipulation_exploitation_m(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Git worktree manipulation exploitation method
  // TODO: Implement specific check for: Git worktree manipulation exploitation method
  // This is a placeholder generated by auto-expand.py

  return findings;
}


} // namespace

std::vector<Scanner> register_autogen_git_repository_security_12_scanners() {
  return {
      {"Git exposed .git directory exploitation method", scan_git_exposed_git_directory_exploitation_m},
      {"Git object file download for source recovery", scan_git_object_file_download_for_source_reco},
      {"Git pack file extraction exploitation technique", scan_git_pack_file_extraction_exploitation_te},
      {"Git hook script injection exploitation method", scan_git_hook_script_injection_exploitation_m},
      {"Git signed commit bypass exploitation technique", scan_git_signed_commit_bypass_exploitation_te},
      {"Git branch protection bypass exploitation", scan_git_branch_protection_bypass_exploitatio},
      {"Git shallow clone limitation exploitation", scan_git_shallow_clone_limitation_exploitatio},
      {"Git worktree manipulation exploitation method", scan_git_worktree_manipulation_exploitation_m},
  };
}

} // namespace apex
