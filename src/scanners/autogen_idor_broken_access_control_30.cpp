/// @file scanners/autogen_idor_broken_access_control_30.cpp
/// @brief Auto-generated scanner: IDOR & Broken Access Control (30)
///        Checks: Bypass IP-based access control, Bypass geo-restriction, Access deprecated API versions, Tenant isolation bypass (multi, Object-level authorization byp
#include "scanner_base.hpp"
#include <regex>
#include <chrono>

namespace apex {
namespace {

/// Bypass IP-based access control
std::vector<Finding> scan_bypass_ip_based_access_control(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Bypass IP-based access control
  auto resp = http.get(base + "/admin");
  if (resp.status_code == 403 || resp.status_code == 401) {
    // Try bypass
    auto bypass = http.get(base + "/admin", {{"X-Original-URL", "/admin"}});
    if (bypass.status_code == 200 && bypass.body.size() > 200 &&
        bypass.body.find("{") == 0) {
      findings.push_back(Finding{"Bypass IP-based access control", "critical", base + "/admin",
                          "Access control bypass successful",
                          "", "X-Original-URL", bypass.body.substr(0, 150)});
    }
  }

  return findings;
}

/// Bypass geo-restriction
std::vector<Finding> scan_bypass_geo_restriction(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Bypass geo-restriction
  auto resp = http.get(base + "/admin");
  if (resp.status_code == 403 || resp.status_code == 401) {
    // Try bypass
    auto bypass = http.get(base + "/admin", {{"X-Original-URL", "/admin"}});
    if (bypass.status_code == 200 && bypass.body.size() > 200 &&
        bypass.body.find("{") == 0) {
      findings.push_back(Finding{"Bypass geo-restriction", "critical", base + "/admin",
                          "Access control bypass successful",
                          "", "X-Original-URL", bypass.body.substr(0, 150)});
    }
  }

  return findings;
}

/// Access deprecated API versions with weaker auth
std::vector<Finding> scan_access_deprecated_api_versions_with_weak(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Access deprecated API versions with weaker auth
  // TODO: Implement specific check for: Access deprecated API versions with weaker auth
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// Tenant isolation bypass (multi-tenant apps)
std::vector<Finding> scan_tenant_isolation_bypass_multi_tenant_app(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Tenant isolation bypass (multi-tenant apps)
  auto resp = http.get(base + "/admin");
  if (resp.status_code == 403 || resp.status_code == 401) {
    // Try bypass
    auto bypass = http.get(base + "/admin", {{"X-Original-URL", "/admin"}});
    if (bypass.status_code == 200 && bypass.body.size() > 200 &&
        bypass.body.find("{") == 0) {
      findings.push_back(Finding{"Tenant isolation bypass (multi-tenant apps)", "critical", base + "/admin",
                          "Access control bypass successful",
                          "", "X-Original-URL", bypass.body.substr(0, 150)});
    }
  }

  return findings;
}

/// Object-level authorization bypass via nested resources
std::vector<Finding> scan_object_level_authorization_bypass_via_ne(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Object-level authorization bypass via nested resources
  std::regex id_re(R"x(/api/[^/]+/(\d+))x");
  
  for (const auto &url : crawl.urls) {
    std::smatch m;
    if (!std::regex_search(url, m, id_re)) continue;
    int id = std::stoi(m[1].str());
    std::string base_path = url.substr(0, m.position(1));
    
    auto r1 = http.get(base_path + std::to_string(id + 1));
    if (r1.status_code == 200 && r1.body.find("{") == 0 &&
        r1.body.find("email") != std::string::npos) {
      findings.push_back(Finding{"Object-level authorization bypass via nested resources", "high", base_path + std::to_string(id + 1),
                          "IDOR: accessed another user\'s data by incrementing ID",
                          "id", std::to_string(id + 1), r1.body.substr(0, 150)});
      return findings;
    }
    break;
  }

  return findings;
}

/// Function-level authorization bypass
std::vector<Finding> scan_function_level_authorization_bypass(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Function-level authorization bypass
  auto resp = http.get(base + "/admin");
  if (resp.status_code == 403 || resp.status_code == 401) {
    // Try bypass
    auto bypass = http.get(base + "/admin", {{"X-Original-URL", "/admin"}});
    if (bypass.status_code == 200 && bypass.body.size() > 200 &&
        bypass.body.find("{") == 0) {
      findings.push_back(Finding{"Function-level authorization bypass", "critical", base + "/admin",
                          "Access control bypass successful",
                          "", "X-Original-URL", bypass.body.substr(0, 150)});
    }
  }

  return findings;
}

/// Access control bypass via URL encoding/double encoding
std::vector<Finding> scan_access_control_bypass_via_url_encoding_d(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Access control bypass via URL encoding/double encoding
  auto resp = http.get(base + "/admin");
  if (resp.status_code == 403 || resp.status_code == 401) {
    // Try bypass
    auto bypass = http.get(base + "/admin", {{"X-Original-URL", "/admin"}});
    if (bypass.status_code == 200 && bypass.body.size() > 200 &&
        bypass.body.find("{") == 0) {
      findings.push_back(Finding{"Access control bypass via URL encoding/double encoding", "critical", base + "/admin",
                          "Access control bypass successful",
                          "", "X-Original-URL", bypass.body.substr(0, 150)});
    }
  }

  return findings;
}


} // namespace

std::vector<Scanner> register_autogen_idor_broken_access_control_30_scanners() {
  return {
      {"Bypass IP-based access control", scan_bypass_ip_based_access_control},
      {"Bypass geo-restriction", scan_bypass_geo_restriction},
      {"Access deprecated API versions with weaker auth", scan_access_deprecated_api_versions_with_weak},
      {"Tenant isolation bypass (multi-tenant apps)", scan_tenant_isolation_bypass_multi_tenant_app},
      {"Object-level authorization bypass via nested resources", scan_object_level_authorization_bypass_via_ne},
      {"Function-level authorization bypass", scan_function_level_authorization_bypass},
      {"Access control bypass via URL encoding/double encoding", scan_access_control_bypass_via_url_encoding_d},
  };
}

} // namespace apex
