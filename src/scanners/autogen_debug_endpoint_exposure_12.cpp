/// @file scanners/autogen_debug_endpoint_exposure_12.cpp
/// @brief Auto-generated scanner: Debug Endpoint Exposure (12)
///        Checks: Ruby on Rails web console endp, Symfony profiler bar accessibl, Flask debugger PIN bypass expl
#include "scanner_base.hpp"
#include <regex>
#include <chrono>

namespace apex {
namespace {

/// Ruby on Rails web console endpoint exposed
std::vector<Finding> scan_ruby_on_rails_web_console_endpoint_expos(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Ruby on Rails web console endpoint exposed
  // TODO: Implement specific check for: Ruby on Rails web console endpoint exposed
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// Symfony profiler bar accessible in production
std::vector<Finding> scan_symfony_profiler_bar_accessible_in_produ(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Symfony profiler bar accessible in production
  // TODO: Implement specific check for: Symfony profiler bar accessible in production
  // This is a placeholder generated by auto-expand.py

  return findings;
}

/// Flask debugger PIN bypass exploitation technique
std::vector<Finding> scan_flask_debugger_pin_bypass_exploitation_t(const Config &, HttpClient &http,
                                       const CrawlResult &crawl) {
  std::vector<Finding> findings;
  if (crawl.urls.empty()) return findings;
  std::string base = base_url_from(crawl.urls[0]);

  // Flask debugger PIN bypass exploitation technique
  auto resp = http.get(base + "/admin");
  if (resp.status_code == 403 || resp.status_code == 401) {
    // Try bypass
    auto bypass = http.get(base + "/admin", {{"X-Original-URL", "/admin"}});
    if (bypass.status_code == 200 && bypass.body.size() > 200 &&
        bypass.body.find("{") == 0) {
      findings.push_back(Finding{"Flask debugger PIN bypass exploitation technique", "critical", base + "/admin",
                          "Access control bypass successful",
                          "", "X-Original-URL", bypass.body.substr(0, 150)});
    }
  }

  return findings;
}


} // namespace

std::vector<Scanner> register_autogen_debug_endpoint_exposure_12_scanners() {
  return {
      {"Ruby on Rails web console endpoint exposed", scan_ruby_on_rails_web_console_endpoint_expos},
      {"Symfony profiler bar accessible in production", scan_symfony_profiler_bar_accessible_in_produ},
      {"Flask debugger PIN bypass exploitation technique", scan_flask_debugger_pin_bypass_exploitation_t},
  };
}

} // namespace apex
